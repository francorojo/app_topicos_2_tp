# app_topicos_2_tp
App using microservices and integration with ai model.

Steps
cd apicore
python3 -m venv .venv
. .venv/bin/activate
pip install Flask
pip install Flask-Limiter
flask --app hello run
